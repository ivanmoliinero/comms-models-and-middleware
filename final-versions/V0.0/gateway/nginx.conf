worker_processes auto;
events { worker_connections 4096; }
http {
    server {
        listen 80;
        location /buy {
            default_type application/json;
            content_by_lua_block {
                local cjson = require "cjson"
                local redis = require "resty.redis"
                local args = ngx.req.get_uri_args()
                local ticket_id = args.ticket_id
                
                if not ticket_id then
                    ngx.status = 400; ngx.say(cjson.encode({error = "Missing ticket_id"})); return
                end
                
                -- Sharding Logic (2 Partitions)
                local hash = ngx.crc32_short(ticket_id)
                local primary_idx = (hash % 2) + 1
                local shard_names = {"shard-a", "shard-b"}
                local target_shard = shard_names[primary_idx]
                
                -- Service Discovery via Sentinels (Using Private IPs)
                local sentinels = {
                    {host = "10.0.1.248", port = 26379},
                    {host = "10.0.1.136", port = 26379},
                    {host = "10.0.1.201", port = 26379}
                }
                
                local function get_master_ip(shard_name)
                    for _, sent in ipairs(sentinels) do
                        local red_sent = redis:new()
                        red_sent:set_timeouts(500, 500, 500)
                        local ok = red_sent:connect(sent.host, sent.port)
                        if ok then
                            local res = red_sent:sentinel("get-master-addr-by-name", shard_name)
                            red_sent:set_keepalive(10000, 10)
                            if res and type(res) == "table" then return res[1], res[2] end
                        end
                    end
                    return nil, "Sentinels unreachable"
                end
                
                local master_ip, master_port = get_master_ip(target_shard)
                if not master_ip then
                    ngx.status = 503; ngx.say(cjson.encode({error = "Service Discovery Failed."})); return
                end
                
                -- Execute Transaction
                local red = redis:new()
                red:set_timeouts(1000, 1000, 2000) 
                local ok = red:connect(master_ip, master_port)
                if not ok then
                    ngx.status = 500; ngx.say(cjson.encode({error = "Connection failed to active master."})); return
                end
                
                local res, err = red:fcall("buy_ticket", 2, "purchased_tracking_ids", "tickets-counter", ticket_id)
                
                if tonumber(res) >= 0 then
                    local wait_res = red:wait(1, 1000)
                    if not wait_res or tonumber(wait_res) == 0 then
                        red:fcall("rollback_ticket", 2, "purchased_tracking_ids", "tickets-counter", ticket_id)
                        ngx.status = 500; ngx.say(cjson.encode({error = "Replication timeout. Purchase rolled back."})); return
                    end
                    ngx.status = 200; ngx.say(cjson.encode({status = "success", ticket = res, shard = target_shard, ip = master_ip}))
                elseif tonumber(res) == -1 then
                    ngx.status = 409; ngx.say(cjson.encode({error = "Ticket already purchased"}))
                else
                    ngx.status = 410; ngx.say(cjson.encode({error = "Tickets sold out"}))
                end
                red:set_keepalive(10000, 100)
            }
        }
    }
}
